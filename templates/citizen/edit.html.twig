{% extends 'base.html.twig' %}

{% block title %}Gérer le Citoyen{% endblock %}

{% block body %}
    <h1>Gérer le Citoyen</h1>

    <div class="page-wrapper bg-gra-03 p-t-45 p-b-50">
    <div class="wrapper wrapper--w790 ">
        <div class="card card-5">
            <div class="card-header">
                <div class="row d-flex justify-content-between align-items-center">
                    <div class="col-auto bg-light">
                        <svg xmlns="http://www.w3.org/2000/svg" width="60" height="60" fill="currentColor" class="bi bi-person-lines-fill text-primary" viewBox="0 0 16 16">
                            <path d="M6 8a3 3 0 1 0 0-6 3 3 0 0 0 0 6zm-5 6s-1 0-1-1 1-4 6-4 6 3 6 4-1 1-1 1H1zM11 3.5a.5.5 0 0 1 .5-.5h4a.5.5 0 0 1 0 1h-4a.5.5 0 0 1-.5-.5zm.5 2.5a.5.5 0 0 0 0 1h4a.5.5 0 0 0 0-1h-4zm2 3a.5.5 0 0 0 0 1h2a.5.5 0 0 0 0-1h-2zm0 3a.5.5 0 0 0 0 1h2a.5.5 0 0 0 0-1h-2z"/>
                        </svg>
                    </div>
                            
                    <div class="col-auto order-3 m-3">
                    {{ include('citizen/_delete_form.html.twig') }}
                  
                    </div>

                    <div class="col bg-light  order-1">
                        <h1 >
                            <span class="text-primary"> <strong> <i> #CityZeN<span class="text-info"> {{ citizen.id }}</i></strong></span>
                            <span class="fs-1 text-secondary"> modifier</span></i>
                        </h1>
                    </div>
                </div>
            </div>


{{ include('citizen/_form.html.twig',  {'button_label': ' Enregistrer'}) }}








    {# <a href="{{ path('app_ressource_index') }}">Retour à l'accueil</a> #}

{% endblock %}
